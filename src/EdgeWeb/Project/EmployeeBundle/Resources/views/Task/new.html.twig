{% extends 'users/layout.html.twig' %}

{% block body_id 'admin_post_index' %}

{% block main %}
   <h3>Add New Task</h3>
    {{ form_start(form, {attr: {novalidate: 'novalidate'}} ) }}
        {{ form_errors(form) }}
        {#{ form_row(form.file,{'attr': {'name': 'file[]' multiple}}) }#}
    {{ form_end(form)}}

    <a href="{{ path('task') }}"><i class="fa fa-arrow-left"></i>Back</a>
{% endblock %}
