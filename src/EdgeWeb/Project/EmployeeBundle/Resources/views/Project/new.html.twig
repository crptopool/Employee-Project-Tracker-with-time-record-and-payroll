{% extends 'users/layout.html.twig' %}

{% block body_id 'admin_post_index' %}

{% block main %}
    <h3 class="btn btn-sm btn-success"><i class="fa fa-plus"></i>Add New Project</h3>
    {{ form_start(form, {attr: {novalidate: 'novalidate'}} ) }}
        {{ form_errors(form) }}
    {{ form_end(form)}}
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script src="{{ asset('bundles/employee/js/calendar.js') }}"></script>
{% endblock %}